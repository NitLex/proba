# Куда вставить доступы

Открой файл **`SECRETS.env`** и заполни:

1. `LEADGID_TOKEN=` — JWT LeadGid (уже может быть заполнен)
2. `ARBTRACK_USERNAME=` — логин от https://trekerarbitrag.ru
3. `ARBTRACK_PASSWORD=` — пароль от трекера

Пример:
```
ARBTRACK_USERNAME=admin
ARBTRACK_PASSWORD=мой_пароль
```

Сохрани файл (`Ctrl+S` / `Cmd+S`) и напиши в чат: **логин вставил**

Токены и пароли в чат не копируй.
